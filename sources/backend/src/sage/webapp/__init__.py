"""HTTP boundary layer: FastAPI app factory and API routers.

Main areas:
- app.py: FastAPI app instance and router registration.
- apis/: one module per API (router.py, schemas.py, service.py) — qa/, health/.
"""
