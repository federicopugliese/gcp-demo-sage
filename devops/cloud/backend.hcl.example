# Copia in backend.hcl (gitignored) e sostituisci con il bucket di state del tuo progetto.
# Il bucket deve esistere prima di `terraform init` — vedi il README per il comando
# `gcloud storage buckets create` da eseguire una tantum (lo fa anche in automatico il
# job "plan" della pipeline .github/workflows/terraform.yml).
bucket = "its-ict-template-sage-tfstate"
prefix = "sage"
