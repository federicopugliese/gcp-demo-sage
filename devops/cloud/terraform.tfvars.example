# Copia in terraform.tfvars (gitignored) e sostituisci con i valori del tuo progetto.
# Tutte le altre variabili (vedi variables.tf) hanno un default sensato per il corso.

project_id = "its-ict-template-sage"
region     = "europe-west6"
